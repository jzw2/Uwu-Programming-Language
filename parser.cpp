#include "parser.h"

namespace naruto
{
	AST::

	AST::parse_var_decl(std::vector<Lex>)
	{

	}

	AST::build_tree(std::vector<Lex> stream)
	{
		for(int l = 0; l < stream.size(); l++)
		{
			if(stream[i].code == TokenCodes.identifier)
			{
				parse_var_decl(
			}
		}
	}
}
